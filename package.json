{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "npx supabase start --ignore-health-check && nuxt dev --host 0.0.0.0",
    "stop": "npx supabase stop --all",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "migra": "supabase db diff --use-migra"
  },
  "dependencies": {
    "@nuxt/ui": "^3.1.3",
    "@nuxtjs/supabase": "^1.4.6",
    "@vite-pwa/nuxt": "^1.0.4",
    "@vueuse/nuxt": "^13.3.0",
    "@vueuse/sound": "^2.1.3",
    "nuxt": "^3.16.0",
    "vue": "latest",
    "vue-router": "latest"
  },
  "devDependencies": {
    "sass-embedded": "^1.83.4",
    "supabase": "^2.9.6"
  }
}
